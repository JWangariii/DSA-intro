// a condition must be valid for the code to execute

// calculate how many times a number can divide by 2

count : INTEGER := 0;

// n is the number to test
WHILE (n%2 = 0) DO
    n := n/2
    count := count + 1;
END_WHILE

write(count)