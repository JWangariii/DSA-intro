// tecket of the bus
// ask how old yo are
// if you are younger than 16 you pay half
// else you pay full

ticket_price := 20;
IF (age <= 16) THEN
    ticket_price := 10;
END_IF

// other way
IF (age <= 16) THEN
    ticket_price := 10;
ELSE
    ticket_price := 20;
END_IF

IF (age <= 10) THEN
    ticket_price := 10;
ELSE_IF (age <= 16) THEN
    ticket_price := 10;
ELSE
    ticket_price := 20;
END_IF